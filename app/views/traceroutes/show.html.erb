<div>
	<h2>Show Traceroute</h2>
	<table>
		<tr>
			<th>Id</th>
			<td><%= @traceroute.id %></td>
		</tr>
		<tr>
			<th>Name</th>
			<td><%= @traceroute.name %></td>
		</tr>
		<tr>
			<th>Description</th>
			<td><%= @traceroute.description %></td>
		</tr>
		<tr>
			<th>Owner</th>
			<td><% if ! @traceroute.user.nil? %>
				<%= @traceroute.user.email %>
			<% end %></td>
		</tr>
		<tr>
			<th>Created at</th>
			<td><%= @traceroute.created_at %></td>
		</tr>
		<tr>
			<th>Updated at</th>
			<td><%= @traceroute.updated_at %></td>
		</tr>
		<tr>
			<th>Participants</th>
			<td>
				<ul><% @traceroute.participants.each do |u| %>
					<li><%= u.email %>
				<% end %></ul>
			</td>
		</tr>      
	</table>
	<ul>
		<li><%= link_to "Edit", {:action => 'edit', :id => @traceroute.id} %>
		<li><%= link_to "Delete", {:action => 'delete', :id => @traceroute.id} %>
		<li> Add point
		<li> List points
	</ul>
</div>
