<%= form_for(@trip) do |f| %>


  <p>This is your private trip. <%= f.submit "Make public",
  															:confirm => "You sure? This is irreversible operation.",
  															:title => "Make #{@trip.name} public" %> </p>  

  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>

