<% if @trip.public && @trip.user != nil %>
<p>Author <%= link_to "#{@trip.user.first_name} #{@trip.user.last_name}", user_path(@trip.user) %></p>
<% end %>
<p><%= @trip.description %></p>

<% if !@trip.votes.empty? %>
<h3>Trip's votes</h3>
<%end%>
<%= will_paginate @votes%>
<ul class="list">
  <%= render @votes %>
</ul>

<%= will_paginate @votes%>

